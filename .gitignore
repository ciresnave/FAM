# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# FAM runtime database (default path is ~/.fam.db, but guard local copies too)
*.db
*.db-wal
*.db-shm

# Agent tooling: opencode workspace (vendors its own node_modules, ~61MB)
.opencode/

# Vendored supertool binary (~540KB, not source)
supertool

# Claude Code local settings and session memory
.claude/settings.local.json
.remember/
