{
  "name": "fam",
  "version": "0.1.0",
  "description": "Federated Agent Messaging - Agent-framework-agnostic messaging for agents, humans, and tools",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "bun run src/scripts/bootstrap.ts",
    "rotate-key": "bun run src/scripts/rotate-key.ts",
    "dev": "bun run src/server/http.ts",
    "start": "bun run src/server/http.ts",
    "broker": "bun broker.ts",
    "server": "bun server.ts",
    "test": "bun test --timeout 60000",
    "typecheck": "bun run tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@noble/hashes": "^2.3.0"
  }
}
